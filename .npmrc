# Nothing special is needed here. The worker is bundled explicitly by
# scripts/build-worker.mjs rather than relying on the bundler resolving
# `new URL('./worker.ts', import.meta.url)`, so no hoisting is required.
